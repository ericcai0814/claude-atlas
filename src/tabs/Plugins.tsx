export function Plugins() {
  return (
    <section className="tab">
      <p className="muted">
        Scaffold — joins <code>enabledPlugins</code> with <code>skill-usage.log</code> to flag
        dead plugins. To be implemented.
      </p>
    </section>
  );
}
