export function Context() {
  return (
    <section className="tab">
      <p className="muted">
        Scaffold — sums always-on rule bytes + memory lines to show context budget pressure. To be
        implemented.
      </p>
    </section>
  );
}
